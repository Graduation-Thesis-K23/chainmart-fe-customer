import React, { memo } from "react";

import styles from "./OrdersShipping.module.scss";

const OrdersShipping = () => {
  return <div className={styles["orders-all"]}>OrdersShipping</div>;
};

export default memo(OrdersShipping);
